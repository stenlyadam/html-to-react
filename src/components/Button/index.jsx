const Button = ({ title }) => {
  return (
    <a
      className="
				    button
					button-primary
					button-wide-mobile
					button-sm"
      href="#"
    >
      {title}
    </a>
  );
};

export default Button;
